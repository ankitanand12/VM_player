import 'package:VSplayer/UI/offline.dart';
import 'package:flutter/material.dart';

void main() => runApp(HotReload());

class HotReload extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return myApp();
  }
}